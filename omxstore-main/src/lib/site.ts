/**
 * OMEX — canonical site origin for SEO (sitemap, canonical links, OG URLs).
 * Change ONE place when moving to a custom domain, and regenerate
 * public/sitemap.xml to match.
 */
export const SITE_URL = "https://omex-store.vercel.app";
